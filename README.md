# SCAL App

This is a SCAL web app

## How to install server

- Install node packages:
    `npm install`
- Run server:
    `node server.js`

## How to install client

- Install node packages:
  `npm install`
- Run client:
  `npm start`